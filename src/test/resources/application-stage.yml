spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
    password: ${METEO_PASSWD}
    platform: mysql
    url: jdbc:mysql://${MYSQL_HOST}:${MYSQL_PORT}/meteo-provider-stage?&serverTimezone=Europe/Moscow&useUnicode=true&characterEncoding=utf8&useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
    username: ${METEO_USER}
